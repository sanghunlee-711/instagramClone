import React from 'react';
import ReactDOM from 'react-dom';
import './index.scss';
import Routes from './components/Routes';

ReactDOM.render(<Routes />,document.getElementById('root'));

// render(무엇을, 여기에 보여줄꺼임)
// ReactDOM.

